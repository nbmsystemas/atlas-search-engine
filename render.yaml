services:
  - type: web
    name: atlas-search-engine-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: ATLAS_ALLOWED_ORIGINS
        value: "https://atlas.nbmsystemas.com"
